server:
  port: 8081
  context-path: /spring

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
